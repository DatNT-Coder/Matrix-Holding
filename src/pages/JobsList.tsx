import { ArrowRight, BadgeCheck, BriefcaseBusiness, Building2, MapPin, Search, WalletCards } from "lucide-react";
import { Link } from "react-router-dom";
import { useEffect, useMemo, useState } from "react";
import { apiGetJobsPage, type Job, type PageResult } from "@/lib/api";
import Pagination from "@/components/Pagination";

const departments = ["Tất cả", "Kinh doanh", "Nhân sự", "Truyền thông", "Hành chính", "Pháp chế"];
const emptyPage: PageResult<Job> = { items: [], page: 1, page_size: 8, total: 0, total_pages: 1 };
const initials = (company: string) => company.split(/\s+/).slice(0, 2).map((word) => word[0]).join("").toUpperCase();
const dateText = (date: string) => new Intl.DateTimeFormat("vi-VN", { day: "2-digit", month: "2-digit", year: "numeric" }).format(new Date(date));
const accentTones = ["border-l-[#4ba4db]", "border-l-[#8b6bd6]", "border-l-[#e3a62c]", "border-l-[#32a085]"];

function CompanyMark({ job }: { job: Job }) {
  return <span className="flex h-14 w-14 shrink-0 items-center justify-center overflow-hidden rounded-xl border border-[#dbe5ef] bg-white text-sm font-extrabold text-blue-brand shadow-sm">{job.company_logo ? <img src={job.company_logo} alt="" className="h-full w-full object-contain p-1.5" /> : initials(job.company_name)}</span>;
}

export default function JobsList() {
  const [result, setResult] = useState<PageResult<Job>>(emptyPage); const [search, setSearch] = useState(""); const [department, setDepartment] = useState("Tất cả"); const [page, setPage] = useState(1); const [loading, setLoading] = useState(true);
  useEffect(() => { setPage(1); }, [search, department]);
  useEffect(() => { const timer = setTimeout(() => { setLoading(true); apiGetJobsPage(page, 8, search, department === "Tất cả" ? undefined : department).then(setResult).catch(() => setResult(emptyPage)).finally(() => setLoading(false)); }, 250); return () => clearTimeout(timer); }, [search, department, page]);
  const companies = useMemo(() => Array.from(new Set(result.items.map((job) => job.company_name))).slice(0, 5), [result.items]);

  return <main className="min-h-screen bg-[#f4f7fb] pb-20"><section className="relative overflow-hidden bg-[#061d3d] py-16 text-white sm:py-20"><div className="absolute inset-0 bg-[radial-gradient(circle_at_88%_15%,rgba(92,169,224,.28),transparent_30%),radial-gradient(circle_at_0%_100%,rgba(26,76,140,.5),transparent_38%)]" /><div className="relative mx-auto max-w-[1220px] px-5 sm:px-7"><p className="inline-flex items-center gap-2 text-xs font-extrabold tracking-[.18em] text-[#bfe5ff]"><BadgeCheck size={15} />MATRIX HOLDING CAREERS</p><h1 className="mt-5 max-w-3xl text-4xl font-extrabold leading-tight tracking-[-.035em] sm:text-5xl">Cơ hội phù hợp cho hành trình tiếp theo của bạn.</h1><p className="mt-5 max-w-2xl text-[15px] leading-7 text-white/75">Khám phá các vị trí từ Matrix Holding và những doanh nghiệp trong hệ sinh thái đối tác.</p><div className="mt-9 grid max-w-5xl gap-3 rounded-2xl bg-white p-3 shadow-2xl md:grid-cols-[1fr_auto]"><label className="flex items-center gap-3 px-3 text-navy"><Search size={21} className="text-blue-brand" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Tìm vị trí, công ty hoặc phòng ban" className="h-12 w-full bg-transparent text-sm outline-none placeholder:text-slate-400" /></label><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-[#f3b83b] px-7 py-3 text-sm font-extrabold text-navy">Tìm việc <ArrowRight size={17} /></button></div><div className="mt-8 flex flex-wrap gap-x-8 gap-y-3 text-sm text-white/70"><span><strong className="mr-2 text-xl text-white">{result.total}</strong>vị trí đang tuyển</span><span><strong className="mr-2 text-xl text-white">{companies.length || "—"}</strong>doanh nghiệp trên trang</span></div></div></section>
    <section className="mx-auto max-w-[1220px] px-5 py-9 sm:px-7"><div className="flex flex-wrap items-center gap-2 border-b border-[#dbe4ed] pb-6">{departments.map((item) => <button key={item} onClick={() => setDepartment(item)} className={`rounded-full px-4 py-2.5 text-sm font-bold transition ${department === item ? "bg-navy text-white shadow-md" : "border border-[#dce5ee] bg-white text-[#52677d] hover:border-blue-brand hover:text-navy"}`}>{item}</button>)}</div><div className="mt-8 grid gap-7 lg:grid-cols-[275px_minmax(0,1fr)]"><aside className="h-fit rounded-2xl border border-[#dce5ee] bg-white p-6"><p className="text-xs font-extrabold tracking-[.14em] text-blue-brand">TÌM VIỆC THÔNG MINH</p><h2 className="mt-3 text-xl font-extrabold leading-snug text-navy">Kết nối với đúng cơ hội, đúng doanh nghiệp.</h2><p className="mt-4 text-sm leading-6 text-muted">Mỗi tin tuyển dụng được cập nhật trực tiếp bởi đội ngũ phụ trách tuyển dụng.</p><div className="mt-6 border-t border-slate-100 pt-5"><p className="text-xs font-bold tracking-[.1em] text-slate-400">DOANH NGHIỆP ĐANG HIỂN THỊ</p><div className="mt-3 space-y-3">{companies.length ? companies.map((company) => <div key={company} className="flex items-center gap-2 text-sm font-bold text-navy"><Building2 size={16} className="text-blue-brand" />{company}</div>) : <p className="text-sm text-muted">Đang cập nhật.</p>}</div></div></aside><div><div className="mb-4 flex items-center justify-between"><p className="text-sm text-muted">{loading ? "Đang cập nhật cơ hội việc làm..." : <>Hiển thị <strong className="text-navy">{result.items.length}</strong> trong số <strong className="text-navy">{result.total}</strong> vị trí</>}</p><p className="hidden text-xs font-bold tracking-[.1em] text-[#718297] sm:block">MỚI NHẤT</p></div>{loading ? <div className="rounded-2xl border border-[#dce5ee] bg-white p-10 text-center text-muted">Đang tải việc làm...</div> : result.items.length === 0 ? <div className="rounded-2xl border border-[#dce5ee] bg-white p-12 text-center"><BriefcaseBusiness className="mx-auto text-slate-300" size={36} /><h2 className="mt-4 font-extrabold text-navy">Chưa có công việc phù hợp</h2><p className="mt-2 text-sm text-muted">Thử chọn một phòng ban hoặc từ khóa khác.</p></div> : <><div className="space-y-5">{result.items.map((job, index) => <Link key={job.id} to={`/tuyen-dung/${job.id}`} className={`group block rounded-2xl border border-[#dce5ee] border-l-4 bg-white p-5 transition hover:-translate-y-0.5 hover:border-[#94c8ec] hover:shadow-[0_14px_30px_rgba(16,45,83,.09)] sm:p-6 ${accentTones[index % accentTones.length]}`}><div className="flex gap-4"><CompanyMark job={job} /><div className="min-w-0 flex-1"><div className="flex flex-col justify-between gap-2 sm:flex-row sm:items-start"><div><h2 className="text-lg font-extrabold leading-snug text-navy transition group-hover:text-blue-brand sm:text-xl">{job.title}</h2><p className="mt-1 text-sm font-bold text-blue-brand">{job.company_name}</p></div><span className="w-fit whitespace-nowrap rounded-full bg-[#edf6ff] px-3 py-1.5 text-xs font-bold text-[#397aaa]">{job.employment_type}</span></div><div className="mt-4 grid gap-2 sm:grid-cols-3"><span className="inline-flex items-center gap-1.5 rounded-lg bg-[#f7fbff] px-3 py-2 text-sm text-[#61758b]"><MapPin size={16} className="text-blue-brand" />{job.location}</span><span className="inline-flex items-center gap-1.5 rounded-lg bg-[#fffaf0] px-3 py-2 text-sm text-[#61758b]"><WalletCards size={16} className="text-[#b77914]" />{job.salary}</span><span className="inline-flex items-center gap-1.5 rounded-lg bg-[#f7f4ff] px-3 py-2 text-sm text-[#61758b]"><BriefcaseBusiness size={16} className="text-[#6f4ec7]" />{job.department}</span></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-muted">{job.summary}</p><div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-4"><span className="text-xs text-slate-400">Đăng ngày {dateText(job.created_at)}</span><span className="inline-flex items-center gap-1 text-sm font-extrabold text-blue-brand">Xem chi tiết <ArrowRight size={16} /></span></div></div></div></Link>)}</div><Pagination page={result.page} totalPages={result.total_pages} onChange={setPage} /></>}</div></div></section>
  </main>;
}
